گزارش کار پروژه

شبیه‌سازی ارتباطات D2D در شبکه‌های 5G/6G با استفاده از چارچوب هوش مصنوعی توزیع‌شده

1. مقدمه

با گسترش شبکه‌های سلولی نسل پنجم و ششم (5G/6G)، افزایش تعداد کاربران و حجم ترافیک داده، چالش‌هایی مانند ازدحام در ایستگاه پایه (Base Station)، کاهش کیفیت سرویس (QoS) و افزایش مصرف انرژی کاربران به وجود آمده است. یکی از راهکارهای پیشنهادی برای مقابله با این مشکلات، استفاده از ارتباطات Device-to-Device (D2D) است که در آن دستگاه‌ها می‌توانند به‌صورت مستقیم یا از طریق رله‌ها با یکدیگر ارتباط برقرار کنند.

در این پروژه، مقاله‌ای با عنوان Dynamic D2D Communication in 5G/6G Using a Distributed AI Framework مورد بررسی قرار گرفته و الگوریتم اصلی آن با نام Enhanced DAIS به‌صورت شبیه‌سازی نرم‌افزاری پیاده‌سازی شده است. هدف از این پروژه، ارزیابی عملکرد این الگوریتم در بهبود کارایی شبکه از نظر نرخ داده و مصرف توان است.

2. هدف پروژه

اهداف اصلی این پروژه عبارت‌اند از:

• شبیه‌سازی یک شبکه بی‌سیم 5G/6G شامل کاربران متحرک

• پیاده‌سازی الگوریتم Enhanced DAIS برای انتخاب هوشمند حالت ارتباطی کاربران

• بررسی تأثیر ارتباطات D2D بر Spectral Efficiency و مصرف توان کل شبکه

• مقایسه حالت ارتباط مستقیم با BS و حالت‌های مبتنی بر D2D

3. معرفی الگوریتم Enhanced DAIS

الگوریتم Enhanced DAIS یک الگوریتم تصمیم‌گیری توزیع‌شده است که در آن هر کاربر با توجه به اطلاعات محلی خود تصمیم می‌گیرد که در چه حالتی فعالیت کند. این تصمیم‌گیری بر اساس معیارهای زیر انجام می‌شود:

• نرخ داده قابل دستیابی (WDR)

• فاصله تا ایستگاه پایه یا رله‌ها

• سطح باتری دستگاه

• سرعت حرکت کاربر

• تعداد رله‌ها و کاربران مجاور

حالت‌های ارتباطی در این الگوریتم شامل موارد زیر است:

• اتصال مستقیم به BS

• کلاینت D2D (D2DC)

• رله تک‌پرشی (D2DSHR)

• رله چندپرشی (D2DMHR)

4. نحوه پیاده‌سازی پروژه

4.1 محیط پیاده‌سازی

این پروژه با استفاده از زبان برنامه‌نویسی Python پیاده‌سازی شده است. دلیل انتخاب Python، سادگی پیاده‌سازی، خوانایی کد و مناسب بودن آن برای شبیه‌سازی‌های سطح سیستم است. برای مدیریت داده‌ها و ذخیره نتایج از کتابخانه pandas استفاده شده است.

4.2 مدل شبکه

در شبیه‌سازی انجام‌شده:

• کاربران به‌صورت تصادفی در یک ناحیه مربعی توزیع شده‌اند

• هر کاربر دارای موقعیت مکانی، سرعت، جهت حرکت و سطح باتری است

• حرکت کاربران در هر گام زمانی به‌صورت خطی مدل شده است

• یک ایستگاه پایه در مرکز ناحیه قرار دارد

4.3 مدل کانال و نرخ داده

برای محاسبه نرخ داده لینک‌ها:

• از یک مدل ساده تضعیف مسیر (Path Loss) استفاده شده است

• نسبت سیگنال به نویز (SNR) محاسبه می‌شود

• نرخ داده با استفاده از فرمول ظرفیت شانون به دست می‌آید

این ساده‌سازی‌ها باعث می‌شود تمرکز پروژه روی منطق شبکه و الگوریتم تصمیم‌گیری باشد.

4.4 پیاده‌سازی الگوریتم DAIS

در هر گام زمانی، برای هر کاربر مراحل زیر انجام می‌شود:

• محاسبه نرخ داده اتصال مستقیم به BS

• شناسایی رله‌های نزدیک در محدوده WiFi و LTE

• محاسبه نرخ داده احتمالی برای هر حالت ارتباطی

• مقایسه نرخ‌ها با توجه به آستانه‌های مقاله

• انتخاب بهترین حالت ارتباطی طبق الگوریتم Enhanced DAIS

این فرآیند به‌صورت کاملاً توزیع‌شده و بدون کنترل مرکزی انجام می‌شود.

5. نتایج شبیه‌سازی

در خروجی شبیه‌سازی، مقادیر زیر در هر گام زمانی ثبت شده‌اند:

• Spectral Efficiency کل شبکه (Mbps)

• مصرف توان کل شبکه

• تعداد رله‌ها

• تعداد کاربران متصل به رله‌ها

نتایج نشان می‌دهند که استفاده از ارتباطات D2D و انتخاب هوشمند حالت ارتباطی می‌تواند باعث افزایش نرخ داده کل شبکه و کاهش بار روی ایستگاه پایه شود.

6. جمع‌بندی و نتیجه‌گیری

در این پروژه، یک شبیه‌سازی شبکه بی‌سیم مبتنی بر D2D برای شبکه‌های 5G/6G پیاده‌سازی شد و الگوریتم Enhanced DAIS مقاله موردنظر مورد ارزیابی قرار گرفت. نتایج شبیه‌سازی نشان می‌دهد که این الگوریتم با در نظر گرفتن شرایط کاربران و لینک‌ها، می‌تواند تصمیمات مؤثری برای بهبود کارایی شبکه اتخاذ کند.

این پروژه نشان می‌دهد که استفاده از روش‌های توزیع‌شده و هوشمند در شبکه‌های آینده نقش مهمی در افزایش بهره‌وری و کاهش مصرف انرژی خواهند داشت.

7. پیشنهادات برای کارهای آینده

• استفاده از مدل‌های دقیق‌تر کانال بی‌سیم

• پیاده‌سازی کامل در محیط‌هایی مانند MATLAB یا NS-3

• مقایسه عملکرد DAIS با الگوریتم‌های دیگر

[  ] بررسی سناریوهای با تعداد کاربران بسیار زیاد (Ultra-dense Network)
